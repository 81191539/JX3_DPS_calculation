# JX3 DPS Calculator 研发计划

## 背景说明

当前项目仍是一个简化期望 DPS 模型，核心目标是用固定循环、期望会心、期望增伤等方式估算傲血天策输出。它不是郭氏逐段取整模型，也不追求在当前阶段完整还原游戏内所有乘区、整数取整、秘籍、奇穴、团队增益和特殊机制。

后续研发目标是逐步把项目骨架迁移为 `React + TypeScript WebUI + TypeScript 计算核心`。公式、数据模型和计算分层主要参考 `jx3dps-online-public`，结果展示和交互体验主要参考 `JX3CalcFE`。

本计划先聚焦傲血天策，不处理 131-133 目标结果校准；134 木桩仍作为主要默认样例与回归基准。

## Phase 0：仓库与文档整理

### 研发目标

- 明确项目当前定位：简化期望 DPS 模型，不是郭氏逐段取整。
- 保持参考项目与主项目边界清晰，避免把大型参考项目纳入主仓库。
- 清理或标注当前与 JX3 DPS 无关的文档内容，降低后续研发误读。

### 主要产出

- README 增加计算模型说明，明确当前模型边界和适用范围。
- `external/` 作为参考项目目录保留在本地，并继续通过 `.gitignore` 排除。
- 对当前与 JX3 DPS 无关的设计文档做删除、归档或显式标注。

### 完成标准

- 新开发者能通过 README 理解当前项目是简化期望模型。
- 主仓库不包含 `external/` 下的参考项目源码。
- 仓库文档中不存在容易误导为当前产品设计的无关内容。

## Phase 1：TypeScript 项目骨架

### 研发目标

- 引入现代前端工程骨架，为后续 WebUI 和计算核心迁移做准备。
- 建立清晰的目录分层，让计算核心、数据、页面功能彼此解耦。
- 保留 Python 版本作为迁移期间的校验参考。

### 主要产出

- 引入 Vite、React、TypeScript 和基础测试工具。
- 建立 `src/core`、`src/data`、`src/features/calculator` 等基础目录。
- 保留 `tiance_dps.py` 和现有 Python 测试，不在本阶段删除旧实现。
- README 增加前端开发、构建和测试命令。

### 完成标准

- 本地可以启动 React 开发服务器。
- TypeScript 项目可以完成一次空壳构建。
- Python 测试仍然通过。
- 新骨架没有改变现有 DPS 计算结果。

## Phase 2：计算核心迁移

### 研发目标

- 将现有 Python 简化期望 DPS 算法迁移到 TypeScript。
- 建立后续可扩展的计算核心类型，不在本阶段追求完整郭氏实现。
- 让 TypeScript 结果与 Python 默认样例保持一致。

### 主要产出

- 属性、目标、技能、增益、循环、结果汇总等基础类型。
- 简化期望 DPS 计算函数。
- 默认 134 样例的 TypeScript 回归测试。
- Python 与 TypeScript 结果对照说明。

### 完成标准

- 默认样例的 DPS、总伤害、技能总伤害与 Python 快照一致或在明确容差内。
- 技能明细求和等于总伤害。
- 不支持的目标等级能给出明确错误。
- TypeScript 单测和 Python 测试均通过。

## Phase 3：傲血天策数据模块

### 研发目标

- 把傲血天策数据从计算逻辑中拆出来，形成可维护的数据模块。
- 先接入当前项目已有能力，避免一次性引入过多机制。
- 为团队增益、技能等级、秘籍、奇穴和特殊机制预留扩展空间。

### 主要产出

- 傲血天策技能数据结构。
- 当前已有技能、循环次数、134 木桩、默认面板数据。
- 简化增益数据和默认配置。
- 数据模块与计算核心之间的稳定接口。

### 完成标准

- 修改默认面板、循环次数或技能参数后，计算核心能稳定产出结果。
- 数据模块不依赖 React 组件。
- 团队增益和完整技能等级数据只预留接口，不在本阶段做全量实现。
- 默认样例回归测试继续通过。

## Phase 4：React WebUI

### 研发目标

- 用 React 计算器替换当前静态 `index.html` 的主要使用体验。
- 第一屏直接提供可用计算器，而不是介绍页。
- 参考 `JX3CalcFE` 的结果展示方式，先实现轻量但完整的傲血天策工作流。

### 主要产出

- 面板属性输入。
- 战斗时间、循环次数和简化增益配置。
- DPS、总伤害、最终面板、技能伤害占比、属性收益展示。
- 桌面端和移动端可用布局。

### 完成标准

- 用户打开页面后可以直接计算默认样例。
- 修改输入后结果能即时或明确触发更新。
- 技能占比与属性收益能从同一套 TypeScript 计算核心获得。
- 页面构建通过，主要文本和控件在常见桌面与移动宽度下不重叠。

## Phase 5：验收与视觉/UI 基线

### 研发目标

- 建立迁移后的质量基线，避免后续公式和 UI 调整破坏默认样例。
- 为 WebUI 建立基础页面验收，确保桌面端和移动端都能稳定使用。
- 固化 README 中的开发、测试、构建和验收流程。

### 主要产出

- Python 回归测试继续保留。
- TypeScript 单测覆盖默认样例、技能汇总、属性收益和错误输入。
- 构建检查覆盖 WebUI 打包。
- Playwright 或等价页面验收覆盖默认计算器页面。
- 桌面端和移动端截图检查，记录主要页面布局基线。
- README 增加验收命令和常见开发流程。

### 完成标准

- `python -m pytest` 通过。
- TypeScript 测试命令通过。
- WebUI 构建命令通过。
- 页面验收能打开计算器并看到默认结果。
- 桌面端和移动端截图无明显文本重叠、控件溢出或空白主区域。
- 默认样例在 Python 与 TypeScript 两侧都有可追踪的回归结果。

## Phase 6：团队增益系统

### 研发目标

- 建立团队增益数据模型，让增益从硬编码参数变为可配置数据。
- 先实现当前简化公式能支持的团队增益，不追求完整郭氏乘区。
- 在 WebUI 中提供清晰的团队增益开关和数值配置入口。

### 主要产出

- 团队增益类型定义，支持开关、覆盖率、层数和数值配置。
- 简化版撼如雷、破风、破甲、虚弱等外功常用增益。
- 团队增益面板，允许用户开启、关闭和调整覆盖率。
- 增益计算结果进入最终面板、DPS 和技能占比。

### 完成标准

- 默认关闭团队增益时，结果与 Phase 5 基线一致。
- 开启单个团队增益时，结果变化方向符合预期。
- 多个团队增益可以组合启用，且配置能被计算核心稳定消费。
- UI 能展示当前启用增益，并能恢复默认配置。

## Phase 7：WebUI 体验对齐参考项目

### 研发目标

- 让 WebUI 从“能算”升级为更接近正式 DPS 计算器产品。
- 参考 `JX3CalcFE` 的结果组织方式，强化配置区、结果区和细节区。
- 建立更清晰的模块导航，支撑后续循环、方案和精确模型功能。

### 主要产出

- 左侧配置、右侧结果的主工作台布局。
- 技能详情、最终面板、收益排行、伤害占比等正式结果视图。
- 方案导入、导出、重置入口。
- 更清晰的模块导航，包括面板、循环、增益、结果等区域。

### 完成标准

- 用户能在一个主工作台中完成输入、计算、查看结果和重置。
- 结果视图能清晰展示总 DPS、技能贡献、属性收益和最终面板。
- 导入、导出、重置不会破坏默认样例。
- 桌面端布局具备正式工具感，移动端保持可用。

## Phase 8：循环与技能数据增强

### 研发目标

- 把 Phase 3 中预留的技能等级、秘籍、奇穴和循环能力逐步变成真实配置。
- 增强循环表达能力，让用户不只修改技能次数，还能编辑循环结构。
- 补齐傲血天策当前模型所需的加速档位数据。

### 主要产出

- 技能等级、秘籍、奇穴配置入口和对应数据结构。
- 傲血天策关键技能的增强数据。
- 循环编辑器，支持添加、删除、排序和调整技能。
- 加速档位数据与循环次数或技能间隔的联动。

### 完成标准

- 默认循环能通过循环编辑器还原。
- 修改秘籍、奇穴或技能等级后，计算结果可追踪地变化。
- 加速档位选择能影响当前模型支持的循环参数。
- 循环配置可以被导出，并能重新导入恢复。

## Phase 9：多方案对比与保存

### 研发目标

- 支持本地保存多套方案，方便对比不同配装、增益和循环。
- 提供直观的差值展示，让用户快速判断改动收益。
- 为未来 JX3Box 配装导入和多心法扩展预留方案结构。

### 主要产出

- 本地方案保存、重命名、复制和删除。
- 多套配装、增益、循环方案对比。
- DPS、总伤害、技能占比、属性收益的差值展示。
- 方案数据版本字段，方便后续结构升级。

### 完成标准

- 用户可以保存当前配置并在刷新后恢复。
- 至少两套方案可以并排对比。
- 差值展示能明确标出提升、降低和无变化。
- 旧方案数据在结构升级时有明确兼容或迁移策略。

## Phase 10：更精确模型

### 研发目标

- 在简化期望模型稳定后，引入更精确的郭氏逐段取整模式。
- 让简化期望模型和郭氏模式并存，用户可以明确选择。
- 使用木桩日志和参考项目公式逐步校准傲血天策结果。

### 主要产出

- `expected` 与 `guoshi` 两种计算模式。
- 郭氏逐段取整的伤害链路，逐步覆盖破防、会心、无双、易伤、非侠和等级减伤。
- 木桩日志校准样例和误差记录。
- 两种模型的结果对比展示。

### 完成标准

- 简化期望模型结果不因引入郭氏模式而变化。
- 郭氏模式有独立测试和默认样例。
- 木桩日志校准能记录样例来源、输入条件、模型结果和误差。
- UI 能明确提示当前使用的计算模式及其适用范围。

## 研发完成后的未来改进点

- 增加 JX3Box 配装导入。
- 增加战斗日志导入和自动校准辅助工具。
- 增加伤害时间曲线、爆发窗口分析和技能覆盖率分析。
- 增加装备特效、小药小吃、阵眼和更多团队增益。
- 完善真实伤害、破招伤害、DOT 快照和特殊机制。
- 增加在线分享链接或导出图片。
- 在傲血天策稳定后，再扩展多心法模块接口。
