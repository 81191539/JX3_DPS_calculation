# JX3 DPS Calculator 研发计划

## 背景说明

当前项目仍是一个简化期望 DPS 模型，核心目标是用固定循环、期望会心、期望增伤等方式估算傲血天策输出。它不是郭氏逐段取整模型，也不追求在当前阶段完整还原游戏内所有乘区、整数取整、秘籍、奇穴、团队增益和特殊机制。

后续研发目标是逐步把项目骨架迁移为 `React + TypeScript WebUI + TypeScript 计算核心`。公式、数据模型和计算分层主要参考 `jx3dps-online-public`，结果展示和交互体验主要参考 `JX3CalcFE`。

本计划先聚焦傲血天策，不处理 131-133 目标结果校准；134 木桩仍作为主要默认样例与回归基准。

## Phase 0：仓库与文档整理

### 研发目标

- 明确项目当前定位：简化期望 DPS 模型，不是郭氏逐段取整。
- 保持参考项目与主项目边界清晰，避免把大型参考项目纳入主仓库。
- 清理或标注当前与 JX3 DPS 无关的文档内容，降低后续研发误读。

### 主要产出

- README 增加计算模型说明，明确当前模型边界和适用范围。
- `external/` 作为参考项目目录保留在本地，并继续通过 `.gitignore` 排除。
- 对当前与 JX3 DPS 无关的设计文档做删除、归档或显式标注。

### 完成标准

- 新开发者能通过 README 理解当前项目是简化期望模型。
- 主仓库不包含 `external/` 下的参考项目源码。
- 仓库文档中不存在容易误导为当前产品设计的无关内容。

## Phase 1：TypeScript 项目骨架

### 研发目标

- 引入现代前端工程骨架，为后续 WebUI 和计算核心迁移做准备。
- 建立清晰的目录分层，让计算核心、数据、页面功能彼此解耦。
- 保留 Python 版本作为迁移期间的校验参考。

### 主要产出

- 引入 Vite、React、TypeScript 和基础测试工具。
- 建立 `src/core`、`src/data`、`src/features/calculator` 等基础目录。
- 保留 `tiance_dps.py` 和现有 Python 测试，不在本阶段删除旧实现。
- README 增加前端开发、构建和测试命令。

### 完成标准

- 本地可以启动 React 开发服务器。
- TypeScript 项目可以完成一次空壳构建。
- Python 测试仍然通过。
- 新骨架没有改变现有 DPS 计算结果。

## Phase 2：计算核心迁移

### 研发目标

- 将现有 Python 简化期望 DPS 算法迁移到 TypeScript。
- 建立后续可扩展的计算核心类型，不在本阶段追求完整郭氏实现。
- 让 TypeScript 结果与 Python 默认样例保持一致。

### 主要产出

- 属性、目标、技能、增益、循环、结果汇总等基础类型。
- 简化期望 DPS 计算函数。
- 默认 134 样例的 TypeScript 回归测试。
- Python 与 TypeScript 结果对照说明。

### 完成标准

- 默认样例的 DPS、总伤害、技能总伤害与 Python 快照一致或在明确容差内。
- 技能明细求和等于总伤害。
- 不支持的目标等级能给出明确错误。
- TypeScript 单测和 Python 测试均通过。

## Phase 3：傲血天策数据模块

### 研发目标

- 把傲血天策数据从计算逻辑中拆出来，形成可维护的数据模块。
- 先接入当前项目已有能力，避免一次性引入过多机制。
- 为团队增益、技能等级、秘籍、奇穴和特殊机制预留扩展空间。

### 主要产出

- 傲血天策技能数据结构。
- 当前已有技能、循环次数、134 木桩、默认面板数据。
- 简化增益数据和默认配置。
- 数据模块与计算核心之间的稳定接口。

### 完成标准

- 修改默认面板、循环次数或技能参数后，计算核心能稳定产出结果。
- 数据模块不依赖 React 组件。
- 团队增益和完整技能等级数据只预留接口，不在本阶段做全量实现。
- 默认样例回归测试继续通过。

## Phase 4：React WebUI

### 研发目标

- 用 React 计算器替换当前静态 `index.html` 的主要使用体验。
- 第一屏直接提供可用计算器，而不是介绍页。
- 参考 `JX3CalcFE` 的结果展示方式，先实现轻量但完整的傲血天策工作流。

### 主要产出

- 面板属性输入。
- 战斗时间、循环次数和简化增益配置。
- DPS、总伤害、最终面板、技能伤害占比、属性收益展示。
- 桌面端和移动端可用布局。

### 完成标准

- 用户打开页面后可以直接计算默认样例。
- 修改输入后结果能即时或明确触发更新。
- 技能占比与属性收益能从同一套 TypeScript 计算核心获得。
- 页面构建通过，主要文本和控件在常见桌面与移动宽度下不重叠。

## Phase 5：测试与验收

### 研发目标

- 建立迁移后的质量基线，避免后续公式和 UI 调整破坏默认样例。
- 保留 Python 测试作为迁移期安全网。
- 为 WebUI 和 TypeScript 计算核心建立最小但有效的验收集。

### 主要产出

- Python 回归测试继续保留。
- TypeScript 单测覆盖默认样例、技能汇总、属性收益和错误输入。
- 构建检查覆盖 WebUI 打包。
- README 增加验收命令和常见开发流程。

### 完成标准

- `python -m pytest` 通过。
- TypeScript 测试命令通过。
- WebUI 构建命令通过。
- 默认样例在 Python 与 TypeScript 两侧都有可追踪的回归结果。

## 研发完成后的未来改进点

- 引入团队增益系统：撼如雷、破风、虚弱/破甲、龙吟·悟、战锋·悟等。
- 引入郭氏逐段取整模式，并与当前简化期望模型并存。
- 补齐傲血天策秘籍、奇穴、技能等级、DOT、破招、真实伤害和特殊机制。
- 增加 JX3Box 配装导入。
- 增加方案保存和方案对比。
- 增加循环编辑器。
- 增加伤害时间曲线和技能详情弹窗。
- 在傲血天策稳定后，再扩展多心法模块接口。
